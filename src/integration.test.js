import {storeFactory} from "../test/testUtils";
import {guessWord} from "./actions";

describe('gyessWord action dispatcher', () => {

    describe('no guessed words', () => {

        test('updates the state correctly for unsuccessful guess', () => {

        });

        test('updates the state correctly for successful guess', () => {

        });

    });

    describe('some guessed words', () => {

        test('updates the state correctly for unsuccessful guess', () => {

        });

        test('updates the state correctly for successful guess', () => {

        });

    });


});